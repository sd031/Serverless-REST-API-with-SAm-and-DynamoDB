AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Serverless REST API with DynamoDB for User Management

Globals:
  Function:
    Runtime: python3.9
    Timeout: 30
    MemorySize: 256
    Environment:
      Variables:
        USERS_TABLE: !Ref UsersTable
        POWERTOOLS_SERVICE_NAME: users-api
    Layers:
      - !Ref DependenciesLayer

Resources:
  # DynamoDB Table
  UsersTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: Users
      AttributeDefinitions:
        - AttributeName: userId
          AttributeType: S
      KeySchema:
        - AttributeName: userId
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST
      StreamSpecification:
        StreamViewType: NEW_AND_OLD_IMAGES
      Tags:
        - Key: Project
          Value: ServerlessUsersAPI

  # Lambda Layer for Dependencies
  DependenciesLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: users-api-dependencies
      Description: Dependencies for Users API
      ContentUri: .
      CompatibleRuntimes:
        - python3.9
      RetentionPolicy: Delete
    Metadata:
      BuildMethod: python3.9

  # API Gateway
  UsersApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: UsersAPI
      StageName: Prod
      Auth:
        ApiKeyRequired: true
      Cors:
        AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS'"
        AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
        AllowOrigin: "'*'"
      TracingEnabled: true
      MethodSettings:
        - ResourcePath: '/*'
          HttpMethod: '*'
          MetricsEnabled: true

  # API Key and Usage Plan
  UsersApiKey:
    Type: AWS::ApiGateway::ApiKey
    DependsOn:
      - UsersApiProdStage
    Properties:
      Name: UsersAPIKey
      Description: API Key for Users API
      Enabled: true
      StageKeys:
        - RestApiId: !Ref UsersApi
          StageName: Prod

  UsersApiUsagePlan:
    Type: AWS::ApiGateway::UsagePlan
    DependsOn:
      - UsersApiProdStage
    Properties:
      UsagePlanName: UsersAPIUsagePlan
      Description: Usage plan for Users API
      ApiStages:
        - ApiId: !Ref UsersApi
          Stage: Prod
      Throttle:
        BurstLimit: 200
        RateLimit: 100
      Quota:
        Limit: 10000
        Period: DAY

  UsersApiUsagePlanKey:
    Type: AWS::ApiGateway::UsagePlanKey
    Properties:
      KeyId: !Ref UsersApiKey
      KeyType: API_KEY
      UsagePlanId: !Ref UsersApiUsagePlan

  # Lambda Functions

  # Create User Function
  CreateUserFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: CreateUser
      CodeUri: src/
      Handler: handlers.create_user.lambda_handler
      Description: Create a new user
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref UsersTable
      Events:
        CreateUser:
          Type: Api
          Properties:
            RestApiId: !Ref UsersApi
            Path: /users
            Method: POST
            Auth:
              ApiKeyRequired: true

  # Get User Function
  GetUserFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: GetUser
      CodeUri: src/
      Handler: handlers.get_user.lambda_handler
      Description: Get a user by ID
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref UsersTable
      Events:
        GetUser:
          Type: Api
          Properties:
            RestApiId: !Ref UsersApi
            Path: /users/{userId}
            Method: GET
            Auth:
              ApiKeyRequired: true

  # List Users Function
  ListUsersFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: ListUsers
      CodeUri: src/
      Handler: handlers.list_users.lambda_handler
      Description: List all users
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref UsersTable
      Events:
        ListUsers:
          Type: Api
          Properties:
            RestApiId: !Ref UsersApi
            Path: /users
            Method: GET
            Auth:
              ApiKeyRequired: true

  # Update User Function
  UpdateUserFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: UpdateUser
      CodeUri: src/
      Handler: handlers.update_user.lambda_handler
      Description: Update a user
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref UsersTable
      Events:
        UpdateUser:
          Type: Api
          Properties:
            RestApiId: !Ref UsersApi
            Path: /users/{userId}
            Method: PUT
            Auth:
              ApiKeyRequired: true

  # Delete User Function
  DeleteUserFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: DeleteUser
      CodeUri: src/
      Handler: handlers.delete_user.lambda_handler
      Description: Delete a user
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref UsersTable
      Events:
        DeleteUser:
          Type: Api
          Properties:
            RestApiId: !Ref UsersApi
            Path: /users/{userId}
            Method: DELETE
            Auth:
              ApiKeyRequired: true

Outputs:
  ApiEndpoint:
    Description: API Gateway endpoint URL
    Value: !Sub 'https://${UsersApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/'
    Export:
      Name: !Sub '${AWS::StackName}-ApiEndpoint'

  UsersTableName:
    Description: DynamoDB table name
    Value: !Ref UsersTable
    Export:
      Name: !Sub '${AWS::StackName}-UsersTable'

  UsersTableArn:
    Description: DynamoDB table ARN
    Value: !GetAtt UsersTable.Arn
    Export:
      Name: !Sub '${AWS::StackName}-UsersTableArn'

  ApiKeyId:
    Description: API Key ID (use AWS CLI to get the actual key value)
    Value: !Ref UsersApiKey
    Export:
      Name: !Sub '${AWS::StackName}-ApiKeyId'
